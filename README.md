# mock-server
すごく簡単なモックサーバ

## start mock server

```bash
node mock-server.js
```

## db.json

```json
[
  {
    "url": "/1",
    "method": "GET",
    "code": 200,
    "data": {
      "name1": "get"
    }
  }
]
```
